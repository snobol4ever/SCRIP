# gc_audit_b_declared.txt -- THE PASS-B HOLDER LEDGER (ARCH-GC § 9 RUNG 3, cfo 2026-09-21, CFO-127).
#
# Pass B is the conservative AUDITOR: it scans the territory pass A scans and names every word that points at a
# block pass A did not mark.  It names CANDIDATES, never defects -- a dead word that looks like a heap pointer
# reads identically to a live root -- so every holder it names is either CURED or DECLARED HERE WITH A REASON.
# ⛔ THE COUNT FALLS ONLY BY CURING OR DECLARING, WHICH IS CONDITION 4 OF LON'S GRANT.  A holder that appears in
# a sweep and is not in this file is a RED in test_gate_gc_conservative_auditor_reports_and_cannot_ship.sh.
#
# ⛔ THE KEY IS A SYMBOL NAME AND NEVER AN OFFSET.  The auditor prints `<module>+0x<offset>` because an offset is
# what it can know at the instant of the finding; the GATE resolves that offset through `nm -S` on the very .so
# that produced it.  An offset is a fact about one build and goes stale on the next; a symbol does not.
#
# ⛔⭐ THE LIMIT THAT BOUNDS EVERY GREEN HERE, MEASURED 2026-09-21 ON A LIVE OPEN DEFECT AND NOT REASONED:
# PASS B'S TERRITORY IS PASS A'S TERRITORY, SO IT FINDS AN UNVISITED WORD INSIDE A VISITED REGION AND CANNOT
# FIND AN UNVISITED REGION.  On user_function_eval_arbno_replace_branch_2 at stress 0, which dies rc=139 with
# the rung-1 trap naming block #1066 (HB_WSC, 32 bytes) RECLAIMED at collection #1, pass B reports findings=0
# over 2234 in-heap words, every one MARKED -- and it still reports 0 with SCRIP_GC_CEILING=0, which widens the
# stack region from 1528 bytes to 4217824.  So the holder of that block is in a REGISTER or in a region nobody
# enumerates (the libc malloc heap, the compile-time arena, the collector's own mmap'd bookkeeping).  A
# findings=0 BOUNDS THE PROBE AND NEVER CLEARS THE COLLECTOR.
#
# FIELDS: <holder-symbol>  <OPEN|DECLARED>  <row or reason>
# OPEN     = a real candidate with a row against it.  The auditor retires when NO OPEN row remains and a sweep
#            finds nothing -- and on that day the gate's expiry arm REDS and says DELETE, because a disabled
#            auditor is a conservative scan wearing an if.
# DECLARED = measured not to be a lost root, with the measurement or the ruling that says so.
#
g_fh                 CURED     2026-09-21 cfo: drv_gc_roots() in src/driver/driver_globals.c visits .name .alias .enc of all 64 FH slots with rt_gc_visit_raw (mark + gc_slot_reg, so the slot RELOCATES), registered in gc_heap.c beside the other eleven; the three standard slots hold string LITERALS and gc_blk_of rejects them.  MEASURED: hb_file_name_unrooted.icn matched its .ref at SCRIP_HEAP_MB=1 at stress 0 1 3 5 where it printed file(aaaa...) before -- and that list is OFF/HARD/softer/softest and NOT ascending severity, because gc_heap.c:266 sets pending every Nth allocation so N=1 is the hard end (coo 2026-09-21, verified in the source here); the sweep counts because it INCLUDES the hard end. ⛔⭐ AND THE COLLECTION COUNT PER ARM, BECAUSE A STRESS LIST STATES A CONFIGURATION AND NOT AN EXERCISE: this witness runs collections=1 at stress 0, 12 at stress 1, 4 at stress 3 and 3 at stress 5 -- TWENTY COLLECTIONS OVER THE WHOLE SWEEP, and the stress=0 arm is ONE. The coo and cto measured the reason jointly: a pending collection only happens at a SAFE POINT, so the stress axis is bounded above by safe-point density (their EVAL witness ARMS 434 times and collects 4). The cure is proven -- the defect reproduced at every one of these points before it and at none after -- but the exercise behind that proof is 20 collections, not a heavy pounding, and it will stay so until the polls land., and pass B no longer names g_fh on ANY of the four declared witnesses.  ⛔ IT WAS ALSO THIS GATE'S DETECTOR PROOF (arm d3) AND THE CURE KILLED THE ARM -- re-pointed the same day to read the OPEN column of this file instead of a hard-coded symbol
g_icn_synth_excl     OPEN      lower_icon.c:24 (and a SECOND static of the same name at :1565) static lc_vec, same class as g_icn_reassigned; named by pass B on the cfo's icon witness procedure_coexpr_suspend_replace_3, not by this gate's four
scan_saved           DECLARED  gen_runtime.c:25, and gen_gc_roots() DOES visit it -- for (i < scan_saved_depth) rt_gc_visit_raw(&scan_saved[i].subj).  Pass B named scan_saved[0].subj on procedure_every_scan_replace_16, which can only mean the depth was 0 at that collection, i.e. a DEAD entry above the live depth: the cstack class exactly.  Deduced from the walk against the finding, not separately instrumented
g_icn_op             OPEN      core.c:382 holds DESCR_t a,b for Icon operator error context between core_icn_op_ctx() and core_icn_op_ctx_clear(); core_gc_roots() does not visit them and core.c:472 reads them after the window
g_icn_reassigned     OPEN      lower_icon.c:54 lc_vec; its .data comes from rt_wsb_alloc (the COLLECTED heap) and lower_gc_roots() visits g_bb_labels and nothing else
pnv.2                OPEN      lower_icon.c:1656 function-scope static lc_vec, same class as g_icn_reassigned
lnv.1                OPEN      lower_icon.c:1658 function-scope static lc_vec, same class as g_icn_reassigned
gnv.0                OPEN      lower_icon.c:1671 function-scope static lc_vec whose .data is handed on as cx.gn, same class as g_icn_reassigned
rtccb                DECLARED  CALLER-SAVED SCRATCH by the cfo's ruling on CTO-153: nothing that must survive a collection may live there, its root registration was a no-op and was deleted, and test_gate_gc_the_caller_saved_spill_block_never_holds_a_heap_reference.sh is the arm that makes that safe
cstack               DECLARED  a word on the C stack below the emitted ceiling pointing at an unmarked block is NECESSARY AND NOT SUFFICIENT (cfo 2026-09-20, CFO-114): a DEAD spill slot holding a stale pointer reads identically, and 12 of the 32 green witnesses print this while answering their oracle
parked               DECLARED  the same class on a parked coexpression stack; only the frame map can say whether the slot is live, and a parked stack's dead frames are exactly where stale pointers accumulate
lnv                  OPEN      lower_icon.c:1702 static lc_vec, the local-name vector the Icon lowerer fills per procedure (reset to n=0 before every use) with pointers to AST strings; named by pass B on the gate's own witnesses 2026-09-24 (9 words); the same class as g_icn_reassigned and g_icn_synth_excl -- a compile-time vector whose words outlive lowering as dead ground; the lowerer-registries row (instrument-test-gate-gc-the-lowerer-registries-are-rooted-not-malloced-...) owns the cure
_excl                OPEN      lower_icon.c:1844 static lc_vec (icn_vec_reuse before every use), the excluded-name vector of icn_collect_implicit_locals; named by pass B 2026-09-24 (9 words); same class and same row as lnv
_impl                OPEN      lower_icon.c:1849 static lc_vec (icn_vec_reuse before every use), the implicit-locals vector; named by pass B 2026-09-24 (1 word); same class and same row as lnv
pnv                  OPEN      lower_icon.c:1700 static lc_vec, the parameter-name vector the Icon lowerer fills per procedure with pointers to AST strings (reset before every use); named by pass B 2026-09-24 (9 words); same class and same row as lnv
ps                   OPEN      lower_icon.c:1597 static lc_vec of const tree_t * (AST node pointers, reset before every use), a lowerer-local vector; named by pass B 2026-09-24 (9 words); same class and same row as lnv
