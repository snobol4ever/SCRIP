diff --git a/src/emitter/emit.cpp b/src/emitter/emit.cpp
index 4cec591e..ee11989a 100644
--- a/src/emitter/emit.cpp
+++ b/src/emitter/emit.cpp
@@ -669,6 +669,7 @@ static int earn_hazard_in(const IR_t * nd, int depth) {
     return 0;
 }
 /*----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
+extern "C" int fc_pair_extent(const IR_t *);                   /* zeta_storage.c -- FLATDISP-LEAF-ORDER: exclusive upper g_emit_cfg index bound of a COND/IMMs OWN inner span, registered at lowering time (lower_snobol4.c:1532/1560, inner allocation ends here); -1 = unregistered. */
 static int frame_need_of(const IR_t * nd) {                                                      /* ⭐ EARN-1 SLICE 1 -- THE ONE AUTHORITY.  THE LAW: a cell needs a frame iff its byte distance to RSP is not a compile-time constant at some reading site.  Verdict map + placeholder caveats documented on op_frame_need (emit.h). */
     if (!nd) return 0;
     switch (nd->op) {
@@ -677,10 +678,23 @@ static int frame_need_of(const IR_t * nd) {
     case IR_MATCH_ASSIGN_SAVE:  {                                                                /* ⭐ s83 SAVE-PROPAGATION FIX: earn_hazard_in walks a node's OWN operand subtree, but SAVE is a pure producer with no relevant operands of its own -- the hazard (a DEFER/VALUE between SAVE and its reader) lives entirely in the PAIRED COND/IMM's operand[0], which SAVE has no pointer to (the link runs the other way: COND/IMM.operands[1] == this SAVE, per lower_snobol4.c:1509/1546).  s82 MEASURED this asymmetry directly (gdb: cur_delta correct, saved_delta garbage) after IMM alone reported need=1 and SAVE's own frame_need_of() unconditionally read 0.  FIX: scan g_emit_cfg->all[] (the SAME whole-graph accessor stmt_has_proc_invoke in bb_match_begin.cpp already uses for an analogous forward-reference walk -- no new global, no new IR_t field, PEERS RULE respected) for the COND/IMM whose operands[1] IS this SAVE, and borrow ITS verdict.  Bounded scan: g_emit_cfg->n is one statement/chain's node count, not the whole program. */
         if (!g_emit_cfg) return 0;
         for (int _i = 0; _i < g_emit_cfg->n; _i++) { IR_t * _c = g_emit_cfg->all[_i];
-            if (_c && (_c->op == IR_MATCH_ASSIGN_COND || _c->op == IR_MATCH_ASSIGN_IMM) && _c->n_operands > 1 && _c->operands[1] == nd) return earn_hazard_in(_c, 0); }
+            if (_c && (_c->op == IR_MATCH_ASSIGN_COND || _c->op == IR_MATCH_ASSIGN_IMM) && _c->n_operands > 1 && _c->operands[1] == nd) { int _old = earn_hazard_in(_c, 0); int _new = frame_need_of(_c); if (getenv("SCRIP_CAP_NEST_DIAG") && _old != _new) fprintf(stderr, "[CAPNEST-SAVE-DIVERGE] c=%p old_haz=%d new_fno=%d\n", (void*)_c, _old, _new); return _new; } }   /* EARN-CAP-NEST FIX: borrow the PAIRED COND/IMM's FULL verdict (frame_need_of, which now includes the nested-span scan below), not just earn_hazard_in -- calling the raw hazard walk here bypassed the fix entirely, since SAVE's own need is defined as "whatever my reader needs". DIAG line added to bisect an unexplained killswitch-off divergence. */
         return 0; }
     case IR_MATCH_ASSIGN_IMM:
-    case IR_MATCH_ASSIGN_COND:  return earn_hazard_in(nd, 0);
+    case IR_MATCH_ASSIGN_COND:  {                                                               /* EARN-CAP-NEST FIX (this session, dv_defer_nested witness): earn_hazard_in(nd,0) walks nd->operands[] -- for COND/IMM that is [inner-pattern ENTRY node, own SAVE] -- so it sees the entry node's OWN operand subtree but NOT the box-chain BETWEEN entry and this node, reachable only by graph ALLOCATION order, not by operand recursion. A capture nested INSIDE another capture's span (e.g. `(*P $ X 'D') . Y`: Y's COND wraps a group whose interior is X's own SAVE/DEFER/IMM/LIT chain) has its hazard invisible to the outer COND's operand walk -- op_frame_need's own doc comment (emit.h:628) named this exact gap ("conditional rows under-detect toward 0") as a known placeholder limitation, unwitnessed until now. MEASURED (printed-value class per s50): dv_defer_nested's oracle is `M BC BCD`; pre-fix HEAD prints `M BC ABCD` -- Y's outer COND read its start-cursor from BEFORE 'A' matched instead of from its own SAVE's delta, because it silently took the ordinary ZRES spine path (need=0) while an activation frame was actually required. FIRST ATTEMPT (superseded, left as history): a raw index-order scan between this node's own index and its SAVE's index in g_emit_cfg->all[] -- FALSIFIED by SCRIP_CAP_NEST_DIAG: COND is allocated BEFORE its own SAVE (lower_snobol4.c's TT_CAPT_COND_ASGN builds `nd` first, `save` second, per its own comment "COND is allocated FIRST"), so the naive [si,ci) range was empty by construction (si=35 > ci=34 on the witness) and the scan silently found nothing. FIX: use fc_pair_extent(nd) -- already registered at lowering time (lower_snobol4.c:1532/1560, "inner allocation ends here") as the exclusive upper bound of THIS capture's own inner span -- as the scan's upper bound, with max(own-index, SAVE-index)+1 as the lower bound (both COND and SAVE precede the inner body in allocation order, whichever of the two is allocated second still precedes the body). ADDITIVE ONLY: h starts as the existing verdict and the scan can only raise 0->1, never lower 1->0. NO NEW GLOBAL (g_emit_cfg and fc_pair_extent's side table both already exist, PEERS RULE respected). Killswitch SCRIP_CAP_NEST=0 restores the pre-fix operand-walk-only verdict byte-identically. */
+        int h = earn_hazard_in(nd, 0);
+        static int _cn = -1; if (_cn < 0) { const char * e = getenv("SCRIP_CAP_NEST"); _cn = (e && *e == '0') ? 0 : 1; }
+        if (_cn && !h && g_emit_cfg && nd->n_operands > 1 && nd->operands[1]) {
+            int _si = -1, _ci = -1;
+            for (int _i = 0; _i < g_emit_cfg->n; _i++) { if (g_emit_cfg->all[_i] == nd->operands[1]) _si = _i; if (g_emit_cfg->all[_i] == nd) _ci = _i; }
+            int _ext = fc_pair_extent(nd);
+            if (getenv("SCRIP_CAP_NEST_DIAG")) fprintf(stderr, "[CAPNEST] nd=%p save=%p cfg_n=%d si=%d ci=%d ext=%d\n", (void*)nd, (void*)nd->operands[1], g_emit_cfg->n, _si, _ci, _ext);
+            if (_si >= 0 && _ci >= 0 && _ext > 0) { int _lo = (_si > _ci ? _si : _ci) + 1, _hi = _ext < g_emit_cfg->n ? _ext : g_emit_cfg->n;
+                for (int _i = _lo; _i < _hi && !h; _i++) { IR_t * _m = g_emit_cfg->all[_i];
+                    if (getenv("SCRIP_CAP_NEST_DIAG")) fprintf(stderr, "[CAPNEST]   scan[%d] op=%d pat_static=%d\n", _i, _m ? (int)_m->op : -1, _m ? _m->pat_static : -1);
+                    if (_m && (_m->op == IR_MATCH_ARBNO || (_m->op == IR_MATCH_DEFER && !_m->pat_static) || _m->op == IR_MATCH_VALUE)) h = 1; } }
+        }
+        return h; }
     default:                    return earn_hazard_in(nd, 0);                                    /* ⭐ s68 SYMMETRY: earn_hazard_in already rules ARBNO / non-static DEFER / MATCH_VALUE to be hazardous material, but the old default:0 consulted it ONLY for the three ASSIGN kinds -- so the SAME node was hazardous as someone else's operand and safe as a node in its own right.  MEASURED over 2917 staged nodes (probe/bb + earn0): exactly 22 carry haz=1 need=0, in exactly two ops -- IR_MATCH_DEFER x20 and IR_MATCH_ALTERNATE x2 -- and those 22 are the entire blast radius of this line.  ALTERNATE is s66's ALT-CAP node, whose 32B carve is real (bb_match_alternate.cpp:65) while fc_geom calls it 0; DEFER's extent is not compile-time known at all (manual Ch.11 Quickscan/Fullscan: matching is exhaustive and deferred expressions are NOT assumed to match at least one character).  Both are precisely 'byte distance to RSP is not a compile-time constant', which is this function's stated LAW.  Conservative by construction: a node with no hazardous subtree still returns 0, so no statement/call carver flips here -- the STATEMENT half of the ignored class needs its own depth-drift predicate and is NOT addressed by this line. */
     }
 }
