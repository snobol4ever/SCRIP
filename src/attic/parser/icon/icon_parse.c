/* ATTIC: dead code excised from src/parser/icon/icon_parse.c (commit base 2a35216) — unreachable from main per --gc-sections oracle. Provenance only; not compiled. */
/*--------------------------------------------------------------------------------------------------------------------*/
tree_t *icn_parse_expr(IcnParser *p) {
    return parse_expr(p);
}
