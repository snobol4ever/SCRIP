/*--------------------------------------------------------------------------------------------------------------------*/
/* ATTIC: dead-code sweep batch 4 (origin src/driver/driver_globals.c, base 4c9b6bd) — stmt_init (static no-op): never called in this TU (scrip.c copy is the live one, called at scrip.c:2141). GC-oracle dead. Provenance only; not compiled. */
static void stmt_init(void) {}
